version:
  get:
    tags:
      - Health
    summary: Api Version
    description: get the api version
    responses:
      '200':
        description: OK
        content:
          text/plain:
            schema:
              type: string
              example: v0.0.1

health:
  get:
    tags:
      - Health
    summary: Api Version
    description: get the api version
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              $ref: models.yaml#/components/schemas/HealthResponse