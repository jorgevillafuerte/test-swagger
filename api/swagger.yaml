openapi: 3.0.0

info:
  title: commerce-integrations
  version: 0.0.1
servers:
  - url: http://localhost:8080
tags:
  - name: Flow
    description: All the operations about flows
paths:
  "/ping":
    $ref: ping.yaml
  "/version":
    $ref: version.yaml
  "/health":
    $ref: health.yaml